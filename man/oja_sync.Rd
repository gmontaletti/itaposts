% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sftp.R
\name{oja_sync}
\alias{oja_sync}
\title{Sincronizza gli snapshot OJA dal server SFTP e li ingerisce}
\usage{
oja_sync(
  region_code = "ITC4",
  snapshots = NULL,
  ingest = TRUE,
  overwrite = FALSE,
  config = oja_sftp_config(),
  path = oja_db_path(),
  lister = oja_remote_list,
  downloader = oja_sftp_download
)
}
\arguments{
\item{region_code}{Codice NUTS-2 da scaricare. Default \code{"ITC4"}.}

\item{snapshots}{Vettore opzionale di \code{snapshot_id} da limitare.}

\item{ingest}{Logico. Se \code{FALSE} esegue solo il download.}

\item{overwrite}{Logico. Inoltrato a \code{\link[=oja_ingest_snapshot]{oja_ingest_snapshot()}}.}

\item{config}{Output di \code{\link[=oja_sftp_config]{oja_sftp_config()}}.}

\item{path}{Percorso del file DuckDB. Default \code{\link[=oja_db_path]{oja_db_path()}}.}

\item{lister, downloader}{Funzioni iniettabili (per i test).}
}
\value{
Invisibilmente un \code{data.frame} con \code{snapshot_id}, \code{downloaded},
\code{ingested}, \code{n_postings}, \code{n_skills}.
}
\description{
Scarica nella cartella locale tutti gli snapshot remoti non ancora
presenti (o solo quelli indicati in \code{snapshots}) e li ingerisce nel DB
DuckDB chiamando \code{\link[=oja_ingest_dirs]{oja_ingest_dirs()}}. La funzione e' idempotente: file
locali con stessa dimensione del remoto vengono saltati, snapshot gia'
presenti in \code{dim_snapshot} non vengono reingeriti a meno di
\code{overwrite = TRUE}.
}
\examples{
\dontrun{
  readRenviron("~/.Renviron")
  oja_sync()
}
}
