% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shim.R
\name{oja_normalised}
\alias{oja_normalised}
\title{Forma compatibile con \code{skillviz::normalize_ojv()}}
\usage{
oja_normalised(
  con,
  snapshots = NULL,
  region_code = NULL,
  years = NULL,
  months = NULL,
  verbose = TRUE
)
}
\arguments{
\item{con}{Connessione restituita da \code{\link[=oja_connect]{oja_connect()}}.}

\item{snapshots}{Vettore opzionale di \code{snapshot_id} da includere.}

\item{region_code}{Vettore opzionale di codici regione.}

\item{years, months}{Vettori opzionali di anni/mesi da intersecare con gli
snapshot disponibili (\code{dim_snapshot.year} / \code{month}).}

\item{verbose}{Logico. Stampa il numero di righe per tabella.}
}
\value{
Una \code{list(postings, skills, companies)} di \code{data.table}.
}
\description{
Ricostruisce dal database DuckDB la lista a tre elementi (\code{postings},
\code{skills}, \code{companies}) attesa dai consumatori a valle. Le \code{postings}
tornano "larghe" con tutte le colonne sorgente; le \code{skills} riportano
i nomi originali in maiuscolo come nella CSV Lightcast; le \code{companies}
riproducono la coppia \code{general_id}/\code{companyname}. Le tre tabelle hanno
chiave su \code{general_id}.
}
\examples{
\dontrun{
  con <- oja_connect()
  on.exit(oja_disconnect(con), add = TRUE)
  ojv <- oja_normalised(con, snapshots = "ITC4_2026_2")
}
}
